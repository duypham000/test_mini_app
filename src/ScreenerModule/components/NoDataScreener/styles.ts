import { css } from '@emotion/react';

const styles = {
  wrapper: () => () =>
    css({
      marginTop: 64,
      display: 'flex',
      flexDirection: 'column',
      alignItems: 'center',
    }),
};

export default styles;
