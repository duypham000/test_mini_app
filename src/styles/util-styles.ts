import variantTypography from './utils/variant-typography'
import scrollBar from './utils/scroll-bar'
import scrollBarMobile from './utils/scroll-bar-mobile'
import table from './utils/table'
import fillIcon from './utils/fill-icon'
import strockIcon from './utils/strock-icon'
import display from './utils/display'

const utilStyles = {
  scrollBar: scrollBar,
  scrollBarMobile: scrollBarMobile,
  table: table,
  fillIcon: fillIcon,
  strockIcon: strockIcon,
  variantTypography: variantTypography,
  display: display
}

export default utilStyles
